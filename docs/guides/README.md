# Guides

How-to documentation for day-to-day tasks lives here as it is written.
Proposed guides:

| Guide | Status |
|-------|--------|
| Setting up the toolchain (devcontainer / local) | see DEVELOPMENT.md |
| Building a kernel image and booting under QEMU | see BUILD.md |
| Writing a Rust subsystem with ports + contract tests | planned |
| Writing a C driver against `aeos_driver.h` | planned |
| Writing a Python module against `sdk/py` | planned |
| Packaging a plugin (`aeos-plugin.toml`) | planned |
| HIL bring-up of a new board | planned |
| Debugging the kernel (GDB stub, serial) | planned |

A guide earns its file when a second person would otherwise have to ask how
to do it twice. Keep guides scenario-based; normative rules belong in
architecture documents, style in CODE_STYLE.md.
